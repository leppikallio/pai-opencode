#!/usr/bin/env bun

// Repo shim.
// Canonical operator CLI is installed via `.opencode/pai-tools/` -> runtime `pai-tools/`.
// Keeping this file preserves existing repo-local invocations.

import "../.opencode/pai-tools/deep-research-cli.ts";
