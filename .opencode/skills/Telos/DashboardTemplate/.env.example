# Preferred: point to a running OpenCode server (TUI counts).
# This allows the dashboard to reuse `opencode auth login` credentials.
OPENCODE_SERVER_URL=http://localhost:4096

# Optional fallback (direct OpenAI calls). Not required if using OPENCODE_SERVER_URL.
# OPENAI_API_KEY=your_api_key_here
