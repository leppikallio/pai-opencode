# PAI (Personal AI Infrastructure) Shell Configuration
# Add these lines to your ~/.zshrc

# ============================================
# PAI Command Alias
# ============================================
# The PAI command launches OpenCode with full PAI context
# This is the primary way to interact with your Personal AI Infrastructure
#
# Usage:
#   pai              # Start PAI in current directory
#   pai "do thing"   # Start PAI with initial prompt
#
# You can alias this to any short command you prefer:
#   alias k="opencode"   # Single letter
#   alias ai="opencode"  # Descriptive
#   alias p="opencode"   # Minimal

alias pai="opencode"

# ============================================
# Optional: Additional aliases
# ============================================
# alias k="pai"  # If you prefer a single-letter shortcut
