BD.md is the OpenCode runtime Beads instruction source.

Treat this file as the canonical runtime-installed Beads instructions document.

When `opencode.json.instructions` includes this file, load it like any other configured instruction source.
